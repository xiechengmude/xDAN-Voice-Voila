# xDAN Hermes 全双工语音 API 服务部署文档

## 1. 系统要求

### 1.1 硬件要求
- CPU: 8核或更高
- 内存: 至少16GB RAM
- GPU: NVIDIA GPU (至少16GB显存)，支持CUDA 11.7或更高版本
- 存储: 至少50GB可用空间

### 1.2 软件要求
- 操作系统: Ubuntu 20.04 LTS 或更高版本 / macOS 12 或更高版本
- Python: 3.11.11
- CUDA: 11.7 或更高版本 (如果使用GPU)
- Git

## 2. 环境准备

### 2.1 安装依赖

#### 2.1.1 安装Python 3.11.11
```bash
# 在Ubuntu上
sudo apt update
sudo apt install software-properties-common
sudo add-apt-repository ppa:deadsnakes/ppa
sudo apt install python3.11 python3.11-dev python3.11-venv

# 在macOS上
brew install python@3.11
```

#### 2.1.2 安装CUDA (仅限GPU环境)
访问 [NVIDIA CUDA下载页面](https://developer.nvidia.com/cuda-downloads) 并按照指示安装CUDA工具包。

### 2.2 克隆代码仓库
```bash
git clone https://github.com/xiechengmude/xDAN-Voice-Voila.git
cd xDAN-Voice-Voila
git checkout api
```

### 2.3 创建虚拟环境
```bash
python3.11 -m venv .venv
source .venv/bin/activate
```

### 2.4 安装依赖包
```bash
pip install --upgrade pip
pip install -r requirements-api.txt
```

## 3. 配置服务

### 3.1 环境变量配置
复制示例环境配置文件并根据需要修改：
```bash
cp .env.example .env
```

编辑 `.env` 文件，根据您的环境调整以下配置：
```
# 服务器配置
HOST=0.0.0.0  # 监听所有网络接口，或设置为特定IP
PORT=8000     # API服务端口

# 模型配置
MODEL_NAME=maitrix-org/Voila-autonomous-preview
AUDIO_TOKENIZER_PATH=maitrix-org/Voila-Tokenizer
REF_VOICE_PATH=examples/character_ref_emb_demo.pkl
DEFAULT_REF_NAME=Homer Simpson

# 会话配置
SESSION_TIMEOUT=1800  # 会话超时时间（秒）
CLEANUP_INTERVAL=300  # 清理过期会话的间隔（秒）
```

### 3.2 模型文件准备
确保以下模型文件已准备就绪：
1. Voila-autonomous-preview 模型
2. Voila-Tokenizer 分词器
3. 参考声音嵌入文件 (character_ref_emb_demo.pkl)

如果需要使用自定义模型，请相应地修改 `.env` 文件中的路径。

## 4. 部署服务

### 4.1 开发环境部署
使用 uvicorn 直接运行服务：
```bash
python run_api_server.py
```

### 4.2 生产环境部署

#### 4.2.1 使用 Gunicorn 部署
安装 gunicorn：
```bash
pip install gunicorn
```

创建 `gunicorn_config.py` 文件：
```python
# gunicorn_config.py
bind = "0.0.0.0:8000"
workers = 1  # 由于GPU资源限制，通常只使用1个worker
worker_class = "uvicorn.workers.UvicornWorker"
timeout = 300
keepalive = 5
```

启动服务：
```bash
gunicorn -c gunicorn_config.py app.main:app
```

#### 4.2.2 使用 Supervisor 管理服务
安装 supervisor：
```bash
sudo apt install supervisor
```

创建配置文件 `/etc/supervisor/conf.d/voila-api.conf`：
```ini
[program:voila-api]
command=/path/to/xDAN-Voice-Voila/.venv/bin/gunicorn -c /path/to/xDAN-Voice-Voila/gunicorn_config.py app.main:app
directory=/path/to/xDAN-Voice-Voila
user=your_username
autostart=true
autorestart=true
stderr_logfile=/var/log/voila-api.err.log
stdout_logfile=/var/log/voila-api.out.log
environment=PATH="/path/to/xDAN-Voice-Voila/.venv/bin"
```

启动服务：
```bash
sudo supervisorctl reread
sudo supervisorctl update
sudo supervisorctl start voila-api
```

#### 4.2.3 使用 Docker 部署
创建 `Dockerfile`：
```dockerfile
FROM nvidia/cuda:11.7.1-cudnn8-runtime-ubuntu20.04

WORKDIR /app

RUN apt-get update && apt-get install -y \
    python3.11 \
    python3.11-dev \
    python3.11-venv \
    python3-pip \
    git \
    && rm -rf /var/lib/apt/lists/*

# 创建虚拟环境
RUN python3.11 -m venv /opt/venv
ENV PATH="/opt/venv/bin:$PATH"

# 复制依赖文件
COPY requirements-api.txt .
RUN pip install --no-cache-dir --upgrade pip && \
    pip install --no-cache-dir -r requirements-api.txt

# 复制应用代码
COPY . .

# 设置环境变量
ENV HOST=0.0.0.0
ENV PORT=8000

# 暴露端口
EXPOSE 8000

# 启动应用
CMD ["python", "run_api_server.py"]
```

构建并运行 Docker 容器：
```bash
docker build -t voila-api .
docker run --gpus all -p 8000:8000 voila-api
```

### 4.3 使用 Nginx 作为反向代理
安装 Nginx：
```bash
sudo apt install nginx
```

创建配置文件 `/etc/nginx/sites-available/voila-api`：
```nginx
server {
    listen 80;
    server_name your_domain.com;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket 支持
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

启用站点并重启 Nginx：
```bash
sudo ln -s /etc/nginx/sites-available/voila-api /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

## 5. 验证部署

### 5.1 健康检查
```bash
curl http://localhost:8000/health
```

预期输出：
```json
{"status":"healthy"}
```

### 5.2 API 文档
访问 Swagger UI 文档：
```
http://localhost:8000/docs
```

### 5.3 简单测试
创建会话：
```bash
curl -X POST http://localhost:8000/api/session/create \
  -H "Content-Type: application/json" \
  -d '{"instruction":"测试指令","voice_name":"Homer Simpson"}'
```

## 6. 监控与维护

### 6.1 日志管理
服务日志位于：
- 开发环境: 控制台输出
- Supervisor: `/var/log/voila-api.out.log` 和 `/var/log/voila-api.err.log`
- Docker: 使用 `docker logs <container_id>` 查看

### 6.2 性能监控
使用 Prometheus 和 Grafana 监控服务性能：

1. 安装 Prometheus 客户端：
```bash
pip install prometheus-client
```

2. 在 `app/main.py` 中添加 Prometheus 中间件（已包含在代码中）

3. 配置 Prometheus 服务器抓取指标

### 6.3 备份策略
定期备份以下内容：
- 环境配置文件 (.env)
- 自定义声音嵌入文件
- 会话数据（如果持久化）

## 7. 故障排除

### 7.1 常见问题

#### 7.1.1 CUDA 相关错误
- 确保 CUDA 版本与 PyTorch 兼容
- 检查 GPU 驱动是否正确安装
- 验证 GPU 内存是否足够

#### 7.1.2 模型加载错误
- 检查模型路径是否正确
- 确保有足够的磁盘空间和内存
- 验证模型文件完整性

#### 7.1.3 API 连接问题
- 检查防火墙设置
- 验证端口是否正确开放
- 确认服务是否正在运行

### 7.2 诊断命令
```bash
# 检查进程状态
ps aux | grep python

# 检查端口占用
netstat -tulpn | grep 8000

# 检查GPU状态
nvidia-smi

# 检查日志
tail -f /var/log/voila-api.out.log
```

## 8. 扩展与优化

### 8.1 水平扩展
对于高负载场景，可以考虑：
- 使用负载均衡器分发请求到多个服务实例
- 为每个实例分配独立的GPU资源
- 使用Redis或其他缓存服务共享会话数据

### 8.2 性能优化
- 调整模型推理参数以平衡质量和速度
- 使用模型量化减少内存使用
- 实现请求队列管理高并发场景

### 8.3 安全加固
- 实现API密钥认证
- 配置HTTPS
- 实现请求速率限制
- 定期更新依赖包

## 9. 参考资源

- [FastAPI 文档](https://fastapi.tiangolo.com/)
- [Uvicorn 文档](https://www.uvicorn.org/)
- [NVIDIA CUDA 文档](https://docs.nvidia.com/cuda/)
- [Supervisor 文档](http://supervisord.org/)
- [Nginx 文档](https://nginx.org/en/docs/)
